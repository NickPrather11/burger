<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}EAT IT AGAIN!{{else}}READY TO EAT!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{id}}">delete</button>
</li>